<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/quiz_nav"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.quizzapp.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/quizQuestionsActivity"
        android:name="com.example.quizzapp.QuizQuestionsActivity"
        android:label="activity_quiz_questions"
        tools:layout="@layout/activity_quiz_questions" />
    <activity
        android:id="@+id/resultActivity"
        android:name="com.example.quizzapp.ResultActivity"
        android:label="ResultActivity" />
</navigation>